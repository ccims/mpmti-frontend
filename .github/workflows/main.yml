name: workflow
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v1
      - name: Prepare
        run: npm ci
      - name: Lint
        uses: stiesssh/tslint-actions@1.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pattern: 'src/**/*.ts'
